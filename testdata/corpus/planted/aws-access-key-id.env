# Deployment credentials, pasted out of the console. This is the shape the
# scanner exists for: the key is in the file, the file is one `cat` away.
AWS_REGION=us-east-2
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_DEFAULT_OUTPUT=json
