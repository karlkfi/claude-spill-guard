# What v0.4.0 is for

**Three things: close the edges the v0.3.0 notes added to the list, put a
control on the release path, and stop the scanner blocking reads of this
repository's own tests.** Decided 2026-09-07, the day v0.3.0 was tagged.

[The v0.3.0 notes](../releases/v0.3.0.md) resolved four operand shapes and,
under *What it does not read*, named the edges a resolver that resolves more
acquires. Those are published, so they are the commitment a reader can hold
this project to without a checkout, and they are the first theme. The second is
the `accruing` row in the store: the release job carries ten named steps and
seven of them assert something no pull request can reach, and v0.1.0 cost four
tag cycles finding that out. The third is friction this repository meets every
session — the hook refuses a `grep` over its own test files, which this groom
hit twice while reading rows — and it is a precision question rather than a
convenience, because thirteen of the twenty-two files that trip it are supposed
to.

The instrument that ranks inside the first theme is new since v0.3.0 and is the
reason v0.3.0 exists: `spill-guard coverage`. It was reset on this machine on
2026-09-07 when the v0.3.0 binary was installed, so its first week of dogfood
runs to about 2026-09-14. On the day it was reset it held 21 records, 15 on the
run-time-operand arm and 6 on the directory arm. **Re-read it before
dispatch**, because the order inside the first table below was set against one
day of that log and the v0.3.0 transcript sweep, not against a week of it.

## The rows the tag waits for

Labelled `v0.4.0` in `docs/queue/`, in the order they rank.

### The resolver's edges, and what the ruleset cannot source

| Row | The disclosed case, or the gap |
|---|---|
| Q149, completed | a `for` loop's variable is poisoned, so `for f in a b; do cat "$f"; done` records both. Upstream resolves it already; this is a port |
| Q151, completed | a `case` arm is read as unconditional, so the resolver scans a path bash never used. Bounded, and a segmenter change stated at the site |
| Q142, completed | the AWS unique-ID table names `ABIA` and `ACCA` and does not name `A3T`. Cut the one arm, and say the other two are sourced |
| Q143, completed | a whitespace-only separator line defeats the PEM body clause. Recall, and the rule's own corpus case has to stay quiet |

### The release path

| Row | What is uncontrolled |
|---|---|
| [Q59](../queue/Q59.md) | signing, attestation and upload have no mutation control. Answered 2026-09-07: the negative control first — verify `checksums.txt` against a wrong certificate identity inside the release job and require failure. The scheduled prerelease is re-asked once one rehearsal has priced its standing cost |
| Q119, completed | nothing retires a release-scope record when its tag is cut. This file is the first instance that gate would have had since v0.2.0's; v0.3.0 shipped with no record at all |
| Q124, completed | nothing re-checks a published body against its notes file after the tag, so an edited notes file diverges until somebody remembers `gh release edit` |
| [Q115](../queue/Q115.md) | the attached provenance bundle has no consumer: neither install script passes `--bundle`, and the release job's own provenance assertions rest on exit status alone |

### The scanner on its own repository

| Row | What it costs today |
|---|---|
| [Q125](../queue/Q125.md) | twenty-two tracked files trip the scanner and thirteen should. Owns the census and decides what a gate over it can be — the design question is where an allowlist lives, and the row says it cannot be *no tracked file may trip the scanner* |
| [Q121](../queue/Q121.md) | one high-entropy `AKIA` literal in `capture_test.go`, the narrow half: move it under `testdata/corpus/vectors/` and read it through `testvec` |
| [Q109](../queue/Q109.md) | the prompt oracle is a recording taken 2026-08-28 against 2.1.238 and nothing re-takes it. Replaying it under `claude -p` needs no credential; a job that does so on a pinned version is what says the `@` grammar has not drifted |

**Eleven rows gated the tag when this was written.** A row marked *completed*
above no longer does; its link goes with the row file the merge deleted. Three
are `M` — Q59, Q125, Q109 — and the rest are one session each. None waits on
anything outside this repository, and the one that waited on the maintainer,
Q59, is answered in its own row.

## The rest of the cluster, labelled and not gating

Four more rows carry the label so that `render --label v0.4.0` shows the whole
of what the release is about, and so cutting the tag drops the label from them.
None of them is a gate, for four different reasons:

- **[Q147](../queue/Q147.md)** — a substitution body after a followed `cd` is
  still recorded. The honest route the row names is a fix upstream in
  `claude-workspace-guard` first, ported by inspection, and a tag cannot wait on
  another repository's merge. 11 of 137 `cd` records in the measured week.
- **[Q133](../queue/Q133.md)** — `jwt` is over half of what the scan still costs.
  Routed `**Measure:**`: the shapes are named and none is measured, so the row
  is a scoping session and not a work item until one of them is.
- **[Q150](../queue/Q150.md)** — the glob table is one machine's options. Routed
  `**Measure:**` for the same reason: whether a snapshot's `shopt` lines track
  the rc file, and whether a hook can find its session's snapshot, both have to
  be driven before there is anything to build.
- **[Q148](../queue/Q148.md)** — the design line still calls the escape hatch an
  environment variable. One word, taken by whoever next opens that section; the
  release-notes half was done in v0.3.0's banner.

## What is out, and why it is named rather than dropped

- **The directory arm** stands on its own argument, in the design under *A
  directory operand is refused rather than walked*. It is the second-commonest
  record in the log and the answer to it is *name the files*, which v0.3.0's
  notes already say.
- **Q92 and Q100** — a quoted assignment arms the override where bash would
  not. Blocked on the same upstream as Q147, and Q100 is the filing that starts
  it; both stay where they rank.
- **Q127**, the MCP file reader, and **Q15**, `!` bash mode. The first is
  undriven and needs a server stood up here; the second needs a person at a
  keyboard. Neither is a code change this release can promise.
- **The scheduled prerelease** half of Q59. It publishes on a timer, and its
  cost is unpriced; the control that lands here is what prices it.

## How this file retires

When `v0.4.0` is tagged. A plan doc naming a release that already exists is the
staleness this arrangement is meant to avoid, so cutting the tag deletes this
file and drops the label from any row still carrying it —
[`release-process.md`](../development/release-process.md) carries that step,
under *Publishing the draft*, and Q119 has shipped the gate that checks it:
`release-scope`. Its mutation control writes its own stale record rather than
planting this file, so nothing in here is load-bearing for CI.
