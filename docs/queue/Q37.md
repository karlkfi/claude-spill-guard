---
id: Q37
rank: al
labels:
    - infra
status: ready
size: S
---

# Re-vendor `queue.py` to upstream `c0a239b`

The copy here is byte-identical to upstream `a055c5e`, which
[`scripts/README.md`](../../scripts/README.md) records and `make vendor` now
holds. One commit has landed on it since: `c0a239b` (2026-08-22), "lint counts
and lists the exhibits it held back". Measured 2026-08-22 against a local
skills checkout — 27 insertions, one deletion, all in `cmd_lint` and its
parser.

`alloc-queue-id.sh` is unmodified and still current at `e52e962`, so this is a
one-file copy and a digest.

**Nothing here is broken by the lag.** The `exhibit:` prefix exempts a citation
from every check, and the upstream change makes a store carrying exemptions
stop reading like one carrying none: a clean `lint` ends `(N marked exhibit(s)
held back)` and a new `--show-exhibits` names each. This store holds no
`file.ext:N` pointers at all, so it holds no exempted ones either — the count
is suppressed at zero, and the line every gate here reads is unchanged. What
the re-vendor buys is the copy staying at zero divergence, which is the state
the record asserts and the only one a reader can check cheaply.

Two things it is worth knowing this row does not depend on:

- **The digest gate cannot find this.** `make vendor` asserts the copy still
  matches what somebody wrote down here; whether upstream has moved needs a
  token this repo does not carry, for the reasons `scripts/README.md` already
  argues. An upstream commit reaches this tree only when somebody looks. This
  row is somebody having looked.
- **It does not touch [`Q30`](Q30.md).** The citation window's blind spot is a
  different silence in the same file, and `c0a239b` reports the waiver rather
  than the window. Settling Q30 is still Q30's job.

`c0a239b` also files an upstream row claiming this repo's `scripts/queue.py`
"matches no upstream revision", from a measurement taken before Q29 merged
here. It is stale as written — the copy has matched `a055c5e` exactly since
`e5dfb3f`. Worth saying upstream when the re-vendor happens, so the two records
agree.
